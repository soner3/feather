server:
  port: 9090

spring:
  application:
    name: authserver
  datasource:
    url: jdbc:mysql://localhost:3306/auth-db
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
    show-sql: true
